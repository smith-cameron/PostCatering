// import { Link } from "react-router-dom"
import React, { useState, useEffect, useContext } from 'react'

const Button = (props) => {
    const { title1, title2, subTitle, link, linkText } = props;
    return (
        <>
        
        </>
    )
}

export default Button;